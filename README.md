# FTFL contract engine

Real contract data for all 10 teams, each team's page themed in that
team's actual colors, a league summary view, and — new this round — a
real backend: commissioner login + edit screen backed by Vercel KV, and a
Fleaflicker proxy for live scores/standings.

**Important:** the commissioner login and Fleaflicker standings only work
once this is deployed on Vercel with the setup below done. Running `npm
run dev` locally (plain Vite) renders the UI fine but the `/api/*` routes
don't exist in that mode — logins will fail and Fleaflicker will show
dashes. Use `vercel dev` (see below) to test the backend locally, or just
deploy and test on the real URL.

## Run it locally (frontend only)

```
npm install
npm run dev
```

## Run it locally with the backend (recommended before you rely on it)

```
npm install -g vercel
vercel dev
```

This runs the `/api` functions locally too, so login and saving actually
work. It'll ask you to link the project to Vercel the first time.

## One-time setup on Vercel

1. **Add a KV database.** In your Vercel project → Storage tab → Create
   Database → KV. Vercel wires the connection env vars in automatically —
   you don't set those by hand.
2. **Set `COMMISSIONER_PASSWORD`.** Project Settings → Environment
   Variables → add `COMMISSIONER_PASSWORD` with whatever password you want
   to use to log in. Never commit this to the repo.
3. **Set `FLEAFLICKER_LEAGUE_ID`** (for live scores/standings). Open your
   league on fleaflicker.com — the URL looks like
   `fleaflicker.com/nfl/leagues/XXXXXX` — that number is the league ID.
   Add it as an env var of the same name.
4. Redeploy after adding env vars (Vercel doesn't hot-apply them to an
   already-running deployment).

## How the data flows now

- On load, the app instantly renders the bundled seed data
  (`src/data/realContracts.ts`) so there's no blank-loading flash, then
  quietly fetches `/api/contracts` and swaps in whatever's actually in KV.
  First time ever, KV is empty, so the API itself falls back to serving
  the seed data — from that point on, KV is the real source of truth.
- The **commissioner screen** (log in via the button top-right of the team
  rail) lets you edit position, toggle taxi/IR for the year you're
  viewing, cut a player, or add a new contract. Nothing is saved to the
  server until you click "Save to server" — up until then it's just local
  state, so you can experiment freely before committing.
- **Auth is intentionally simple**: one shared password in an env var,
  checked server-side on every write. Good enough for "only the
  commissioner edits this," not a real multi-user permission system.

## Fleaflicker integration

Your league ID is `245051`. Set it as `FLEAFLICKER_LEAGUE_ID` on Vercel and
you're connected — no auth needed, it's a public read-only API.

The response field mapping (`record_overall.wins`, `points_for.value`,
etc.) in `src/lib/api.ts` is confirmed against Fleaflicker's official API
docs at fleaflicker.com/api-docs, not guessed. I wasn't able to fetch your
specific league's live data directly to double-check real values while
building this, so if anything still looks off after you set the env var
and redeploy, it'd be a genuinely unexpected API quirk rather than a
known gap — tell me what the Network tab shows for
`/api/fleaflicker?endpoint=FetchLeagueStandings&season=2026` and I'll fix
it fast.

Only standings (records + points) are wired into the UI right now.
`FetchLeagueScoreboard` (live weekly matchups) is already allowed through
the proxy but not called from anywhere yet.

## What's real vs. generated

- `src/lib/contracts.ts` — the confirmed rules: new contracts escalate
  by tier at signing (under $10 → +$2/yr, $10–29 → +$3/yr, $30+ →
  +$5/yr); cutting a player costs 50% of their current salary, applied
  across every remaining contract year, rounded up once at the end (see
  the "Cut penalty — corrected" section below for the fix history); taxi squad and IR don't count against the $200 cap;
  "resign" cost projects the final rate forward using that rate's
  increment for as many years as the player's been held at it.
- `src/data/realContracts.ts` — generated by `scripts/import-from-excel.py`
  from `FTFL_Dynasty_League.xlsx`. This is now just the SEED for KV, not
  the live source once you've saved anything through the commissioner
  screen.
- `src/data/teams.ts` — every color is a raw hex value read directly off
  the workbook's actual cell fills and font colors (via `openpyxl`), not
  adjusted or brightened. A couple of teams' real accent color only shows
  up as font color, not fill (Denver's green, for example) — worth
  knowing if you ever re-run the extraction for a team that looks off.

## Next steps (not built yet)

1. Trade tool — right now moving a player between teams means editing
   `team` on the contract directly; a proper two-sided trade builder that
   swaps players/picks atomically isn't built.
2. Live matchup scores (data's available via the proxy, just not
   displayed yet).
3. Draft pick tracking, transition tags — not touched yet.

## If a rule needs to change

Escalation and cut-penalty math live in `src/lib/contracts.ts`
(`annualIncrement`, `cutPenalty`, `projectedResignCost`). Change the
number there, not in the UI.

## Cut penalty — corrected

Fixed. Cutting a player costs 50% of their CURRENT salary (the rate in
the year of the cut), applied flatly across every remaining contract
year, with ONE rounding at the end — not the escalating future rate for
each year, each rounded separately. Example: a $5 current salary with 1
year remaining → ceil(5 × 0.5 × 1) = $3. See `cutPenalty` in
`src/lib/contracts.ts`.

## Free agent review — new

Commissioner-only panel (📋 FA Review button, next to the login button)
that pulls Fleaflicker's transaction log and cross-references every
free-agent pickup against contracts already on file. Anything without a
matching contract gets an inline form — team, position, cost, length —
one click to add. Save to server when done.

**Honesty note on this one:** the team/player-matching logic and the
"already has a contract" check are 100% your own data, fully verified.
The part reading *into* Fleaflicker's transaction items (figuring out
which item is a free-agent add vs. a trade vs. a cut, and what the bid
amount was) is built from an educated guess at their field names — the
exact schema is documented on their site but is long enough that I
couldn't retrieve the specific section confirming it byte-for-byte. If
the FA Review page shows nothing when you know pickups happened, or shows
useless-looking data, open the "Other recent activity" section at the
bottom of that page — every item's raw JSON is shown there — and paste me
one real example. That's a five-minute fix once I can see actual field
names instead of guessing.

**Trades, cuts, and taxi/IR from Fleaflicker are NOT auto-applied.** Same
reason — I'd rather not silently move players around based on an
unverified schema. They still show up in "Other recent activity" for
visibility, but for now, use the existing Cut / Taxi / IR buttons on each
team's Edit Roster screen (already reliable, already tested) to apply
them manually. Once the transaction schema is confirmed, this is the
natural next thing to make one-click.

## Trade control — added

Each contract row in edit mode now has a "Trade to…" dropdown, alongside
Taxi / IR / Cut. Picking a team reassigns that contract to it instantly
— same salary, same years, same everything, just a new owner. This was
missing before; there was no way to move a player between teams short of
cutting and re-adding (which would have lost the real contract terms).

## The manual workflow, end to end

Since Fleaflicker trades/cuts/taxi/IR aren't auto-applied (see above),
here's what actually needs to happen when something happens in
Fleaflicker:

1. Log in as commissioner.
2. Go to the team page for whoever's affected, click "Edit roster."
3. **Cut** → click Cut next to that player.
4. **Trade** → pick the receiving team from "Trade to…" next to that
   player. Do this once per player moving (a 3-for-2 trade is 5 clicks,
   not one "propose trade" flow — there's no multi-asset trade builder
   yet).
5. **Taxi/IR move** → click Taxi or IR next to that player for the year
   you're viewing.
6. **New free agent signing with a cost** → use the FA Review page
   instead (it's built for this specifically) or "Add contract" at the
   bottom of the team page.
7. Click **"Save to server"** — nothing above persists until this is
   clicked. You can make several changes across the same edit session
   before saving once.

## Fleaflicker schema — now confirmed against real data, not guessed

Nick pasted a live response from `/api/FetchLeagueTransactions`. Two big
findings:

1. **Field names are camelCase over the wire** (`timeEpochMilli`,
   `recordOverall`, `proPlayer`, `nameFull`) even though Fleaflicker's own
   docs describe them in snake_case (`time_epoch_milli`,
   `record_overall`). This is a standard protobuf-JSON quirk — the docs
   show the underlying proto field name, but serialization auto-converts
   to camelCase. **This means `fetchFleaflickerStandings` was broken from
   the start on a naming mismatch** — fixed now, so Record/PF on the
   League Summary page should actually populate.
2. **Confirmed drop detection.** A dropped player shows up as
   `item.transaction.type === "TRANSACTION_DROP"` with the player at
   `item.transaction.player.proPlayer.nameFull` and the team at
   `item.team.name` (a sibling of `transaction`, not nested inside it).
   The FA Review page now shows a "Confirmed drops" section — any
   Fleaflicker drop that matches an existing contract gets a one-click
   "Confirm cut" button.

**Still not confirmed:** the exact `type` string for an add (no example
came through in the sample), and whether trades show up as a clean event
at all — several items in the sample had a player + an `owner` field but
no `type`, which look like "this player is on X's trade block" context
rather than a completed trade. Those are filed under "Other recent
activity," not acted on. If a real trade shows up in that section with a
recognizable shape, send it over and trade auto-detection can get built
properly instead of guessed.

No bid/waiver-cost field appears anywhere in the real data, matching what
Nick said — this league prices FAs outside Fleaflicker. The FA-add form
no longer pretends to pre-fill a cost from Fleaflicker.

## Commissioner login — removed for now

The password wasn't working reliably, so rather than keep debugging it,
the whole gate is off: edit controls (Edit roster, FA Review, Save to
server) are open to anyone with the link. No login screen, no password.

What changed:
- `api/_lib/auth.ts` — `checkCommissionerPassword` always returns `true`
  now, so the write endpoint (`api/contracts.ts`) accepts any save.
- `src/App.tsx` — the login button/panel is gone; `isCommissioner` is
  just `true`.
- `src/lib/api.ts` — `saveContracts` no longer sends or checks a
  password. `login`/`getStoredPassword`/`clearStoredPassword` were
  removed since nothing calls them anymore.

**To re-lock this later:** restore the real check in
`checkCommissionerPassword` (compare `req.headers['x-commissioner-password']`
against `process.env.COMMISSIONER_PASSWORD`), and bring back a small
login form that POSTs to `api/auth.ts` (that endpoint was left as-is and
still works, it's just unused right now) and stores the password in
`sessionStorage` for subsequent saves.

## Trades, cuts, taxi/IR — now fully automatic (one button, not per-item)

New: `api/sync.ts` + a "🔄 Sync trades / cuts / taxi / IR" button on the
FA Review page. One click reconciles every contract against Fleaflicker's
CURRENT rosters — not by reading an event log and guessing what it means,
but by comparing "what does my data say" against "what does Fleaflicker
say right now":

- Player not on ANY team's roster anymore → **cut** (contract removed).
- Player on a different team than my records show → **traded** (contract
  reassigned, same salary/years intact).
- Player in Fleaflicker's TAXI or INJURED roster slot → **taxi/IR flag**
  set or cleared for the year being synced.

No per-player confirmation — it applies everything and saves immediately,
then shows a plain-text summary of what changed. FA pickups are the only
thing still requiring you: Fleaflicker has no price data for adds in this
league, so those still show up below as a manual "enter a cost" form.

**Confidence level, honestly:** the "cut" and general approach reuse the
confirmed FetchLeagueTransactions schema from before. The roster
reconciliation (`FetchLeagueRosters`, in `api/sync.ts`) is built by
extending a schema I DID confirm for a single team (`FetchRoster`) to its
all-teams sibling, which was never seen in a real response — it's a
well-grounded guess, not a wild one, but it's the one part of this whole
project that's still unverified against live data. If you click Sync and
it always reports zero changes even when you know a trade or cut just
happened, that's the first thing to check: hit
`/api/fleaflicker?endpoint=FetchLeagueRosters` directly in your browser
and send me what comes back — same as the transactions fix, this is a
five-minute correction once I can see the real shape instead of a
best-effort extrapolation.

## Stadium art — 6 of 10 teams

Real generated stadium images now render in each team's header, replacing
the "coming soon" placeholder — Boulder Bandits, Broad Ripple Big Horns,
Olde Town Osos, South Bend Silver Hawks, Summit County Ski Bums, and
Wakarusa Wizards. Images live in `public/stadiums/`, wired up via the new
`stadium` field on each team in `src/data/teams.ts` (name, made-up
capacity, image path). The other 4 teams keep the original placeholder
until their art exists — nothing needed to add them later beyond filling
in that same field.

Capacities are made up, not real data — Nick asked for plausible numbers,
not anything sourced.

One open item: Broad Ripple Big Horns' stadium had no name anywhere in
the source image, so it's currently labeled "Ram Rock Coliseum" as a
placeholder — one line to change in `teams.ts` if a different name is
wanted.

## Backend storage — Redis, not "Vercel KV"

Correction from an earlier version of this README: Vercel's Storage
marketplace has moved on from the old REST-based "Vercel KV" product
(which needed `KV_REST_API_URL`/`KV_REST_API_TOKEN`) to provisioning
plain Redis instead, which only gives you a single `REDIS_URL`
connection string. `api/_lib/store.ts` uses the plain `redis` npm
package against that directly — no other setup needed beyond creating
the database in Vercel's Storage tab and connecting it to this project;
the `REDIS_URL` env var gets added automatically when you do.

If Sync or Save to server ever fail with a message mentioning `REDIS_URL`,
that's this piece — check it's actually present in Environment Variables
and that the database shows as connected in the Storage tab.

## Incident: sync mass-cut the whole league — fixed with real safety guards

What happened: the `FetchLeagueRosters` schema in `api/sync.ts` was always
labeled as an unverified guess (see the comment above `fetchRosterMap`),
and it turned out to be wrong — the roster map came back empty, and the
code treated "no match found" as "this player isn't rostered anywhere,"
cutting every single contract in the league in one sync. That's a real
failure of judgment on my part: an unverified data source should never
have been allowed to drive a fully-automatic destructive action with no
circuit breaker. Fixed properly now, not just patched:

1. **Guard #1 — empty/near-empty roster data refuses to run at all.**
   If Fleaflicker's response produces fewer than 5 recognizable players,
   `api/sync.ts` aborts before touching any data and returns the raw
   response sample in the error, so the actual shape is visible instead
   of guessed at again.
2. **Guard #2 — implausible cut ratio refuses to save.** Even with a
   healthy-looking roster map, if the computed result would cut more
   than 25% of the active roster in one pass, the sync aborts and lists
   which players it would have cut, instead of saving. A real week never
   looks like this; a name-matching bug sometimes does.
3. **`api/restore-seed-data.ts` — new emergency recovery endpoint.**
   Visiting `/api/restore-seed-data` in a browser wipes whatever's in
   Redis and replaces it with the original spreadsheet import. Bookmark
   this. It's the fast way back to a known-good state if anything ever
   corrupts live data again, without needing to find Upstash's console
   under time pressure.

The underlying schema question — what `FetchLeagueRosters` actually
returns — is still unconfirmed. These guards mean that not knowing is
now safe to find out (it aborts loudly instead of failing destructively),
but the root cause fix still needs a real response sample. If Sync ever
refuses to run and shows a "raw sample" in the error, that's the payload
to send over.

## Roster schema — now confirmed for real (cuts/trades), taxi/IR still a guess

The safety guard did its job: it refused to run on bad data and surfaced
a real sample instead. Turns out `FetchLeagueRosters` has no
groups/slots nesting at all — it's flat: `{ rosters: [{ team, players:
[{ proPlayer: { nameFull, ... } }] }] }`. Fixed `fetchRosterMap` in
`api/sync.ts` to match this confirmed shape. Verified directly against
Nick's real sample (Jordan Love → Boulder Bandits) before shipping —
this part is no longer a guess.

**Taxi/IR is still unconfirmed** — the sample was truncated before
reaching whatever field marks a player as taxi-squad or IR on this
endpoint. Current code checks a few plausible field names
(`rosterSlot.name`, `slot.name`, `status`, `isTaxi`, `isInjuredReserve`)
and safely defaults to "neither" if none match — so it won't wrongly tag
anyone, it just might not catch a real taxi/IR move yet. If Sync doesn't
seem to be picking up a taxi/IR change you know happened, that's the
next thing to nail down — ideally with one more real sample of a player
who's actually on taxi or IR right now, so the differentiating field is
visible in full (not truncated).

## Second incident: suffix mismatches wrongly cut real players

The roster schema fix was correct, but name matching itself was too
strict — a plain lowercase/trim comparison, which fails whenever the two
sources format a suffix differently ("Michael Pittman Jr." vs "Michael
Pittman"). This wrongly cut several real, actively-rostered stars
(Mahomes, Pittman, Etienne, Penix, Walker) even though the 25% safety
threshold didn't trigger (real drops mixed in with the false positives
kept it under the line).

Fixed in both `api/sync.ts` and `src/App.tsx` (FA Review's contract
matching): `normalize()` now strips periods/commas/apostrophes, accents,
and generational suffixes (Jr/Sr/II/III/IV) entirely from both sides
before comparing, verified against every false-positive name from the
actual incident before shipping.

**Worth considering, not yet built:** given two real incidents from two
different root causes, a "review cuts before saving" confirmation step
(cuts only — trades/taxi/IR are non-destructive and can stay fully
automatic) would add a safety checkpoint against whatever the next
unforeseen name-matching edge case turns out to be, at the cost of one
extra click when a cut is real. Worth deciding on deliberately rather
than defaulting to full-auto by inertia.

## Third incident, and the real fix: cuts are no longer automatic

Two things went wrong in the same sync run:

1. **Taxi/IR detection was actively erasing real data.** The guessed
   field names for taxi/IR status never matched anything real, so
   `isTaxi`/`isIR` always computed to `false` — which meant every
   legitimate taxi player got read as "not taxi" and had their flag
   removed on every single sync. **Taxi/IR detection is now removed
   entirely** (not just "safer," gone) until a real sample confirms the
   actual field. Set taxi/IR manually on the team page for now.
2. **A genuinely-still-rostered player (Jacoby Brisset) came back
   missing from Fleaflicker's roster data**, and got treated as a cut.
   This isn't the suffix bug — likely the endpoint doesn't return full
   benches, or needs a parameter this code isn't passing. Root cause
   still open.

Given "not found on any roster" has now caused real damage from two
unrelated causes, the actual fix is architectural, not another patch:
**cuts are no longer applied automatically at all.** `api/sync.ts` still
computes them, but only returns them as proposals — nothing is removed
until the commissioner clicks "Confirm cut" on the FA Review page for
that specific player, with a "not a real cut, ignore" option right next
to it for false positives. Trades stay automatic (wrong trades are
easily fixed manually; lost contract data isn't).

This is the actual permanent guard, not a promise to be more careful
next time: no code change can silently delete a contract again, because
the deletion now requires a human clicking a button that names the
specific player.

## Root cause finally found: 7 spelling typos, not a systemic problem

Nick pasted the ENTIRE FetchLeagueRosters response (all 10 teams, every
player) after a sync showed a wall of cuts he knew were wrong. Cross-
checked all 232 active-2026 contracts against it programmatically:

**225 matched immediately. Exactly 7 didn't, and every one was a genuine
typo in the original spreadsheet** — not a suffix issue, not a missing-
roster issue, nothing systemic:

- Jacoby **Brisset** → Jacoby **Brissett**
- **Kenneth** Gainwell → **Kenny** Gainwell
- **Deoin** Burks → **Deion** Burks
- Jayl**e**n Noel → Jayl**i**n Noel
- Jayd**e**n Ott → Jayd**n** Ott
- **Rchaad** White → **Rachaad** White
- Bren**a**n Thompson → Bren**e**n Thompson

Fixed all 7 directly in `src/data/realContracts.ts`. Re-ran the same
check afterward: **232/232 match. Zero mismatches, zero false cuts, zero
real cuts or trades pending either** — the entire "sync deleted
everyone" scare was 100% these 7 typos, nothing more.

**This is genuinely good news for trusting cuts again**: the name-
matching logic itself is sound — three different underlying problems
(empty roster map, missing suffix handling, and now plain spelling
typos) all looked identical from the outside ("not found on any
roster"), which is exactly why the confirm-before-cut safety net was the
right call while the cause was still unknown. Now that it's isolated and
fixed, a real sync should show zero proposed cuts unless something
actually happens in the league.

**Bonus, low-risk improvement while in here:** added confirmed real
`fleaflickerId` per team to `src/data/teams.ts`, extracted directly from
this same response. Not used yet, but sets up matching teams by ID
instead of by name string in the future, which removes an entire class
of potential name-mismatch bugs for teams (not just players).

## Taxi/IR — still genuinely unresolved, and here's exactly why

Went through the full roster dump specifically looking for a current-
taxi/current-IR field on each player. It isn't there. Every player has
an `eligibleFor` array listing what slots they're ELIGIBLE to occupy
(and it turns out nearly every player is taxi-eligible and every injured
player is IR-eligible) — but nothing on this endpoint says which slot
they're ACTUALLY in right now. Eligible-for isn't the same as
assigned-to, and this bulk, all-teams endpoint doesn't expose the
assignment at all.

The single-team endpoint (`FetchRoster`, needs a `team_id`) is
officially documented to return `groups: [{ group: "TAXI"/"BENCH"/
"START"/"INJURED", slots: [...] }]` — actual current placement, not
eligibility. That's the right endpoint for this; the docs schema was
already confirmed by Fleaflicker, it just hasn't been tested against a
real response yet. Now that real fleaflickerId values exist in
teams.ts, this is a straightforward next step: fetch
`FetchRoster?sport=NFL&league_id=245051&team_id=1417685` (Boulder
Bandits, Nick's own team — likely to return full detail since he's
authenticated as its owner) and confirm the shape before wiring it up,
same discipline as everything else in this file.

## Taxi/IR — now confirmed and fully automatic

Nick fetched `FetchRoster?sport=NFL&league_id=245051&team_id=1417685`
directly (the single-team endpoint) and pasted the complete response.
Confirmed exactly: `{ groups: [{ group: "START"/"INJURED"/"TAXI"/
undefined-for-bench, slots: [{ leaguePlayer: { proPlayer: { nameFull }
} }] }] }`. Checked against known players before shipping: Zach
Charbonnet sits in the group with `group:"INJURED"`, Cam Ward and Oscar
Delp both sit in the group with `group:"TAXI"` — exactly matching what
the contract data already said they should be.

`api/sync.ts` now calls this per-team endpoint once for each of the 10
teams (using the real `fleaflickerId` values added to `teams.ts`
earlier) instead of the old bulk `FetchLeagueRosters` call. This one
data source now drives trades AND taxi/IR together, since team_id
itself tells us ownership. **Trades and taxi/IR apply and save
automatically.** Cuts remain proposals requiring one click to confirm —
that safety net stays regardless of how reliable the rest of this has
proven, since a cut is the one truly irreversible action.

If any single team's fetch fails, the whole sync aborts rather than
guessing that team's players got cut — 10 network calls means 10 chances
for a transient failure, and a partial result would be worse than no
result.

## Taxi/IR automation reverted — real wall found, not a bug to patch

The confirmed `FetchRoster` schema from the last round was real, but the
endpoint itself turned out to require an authenticated Fleaflicker
session. Proof: the exact same team_id that worked when Nick fetched it
from his own logged-in browser (Boulder Bandits, 1417685) returned
HTTP 400 when this server called it — same team, same URL shape, only
difference is *who* asked. All 10 teams failed identically, confirming
this isn't a per-team issue or a missing parameter, it's that this
server has no way to log in as Nick.

Fleaflicker's public/anonymous endpoints (`FetchLeagueStandings`,
`FetchLeagueTransactions`, `FetchLeagueRosters`) all keep working fine —
this one specific endpoint just isn't reachable from an unauthenticated
server. Reverted `api/sync.ts` to the bulk `FetchLeagueRosters` endpoint
only (proven: 232/232 contracts matched against real data). Trades and
cut-proposals work exactly as before. **Taxi/IR is back to manual, on
the team page**, and stays there unless a way to authenticate this
server as a real Fleaflicker session shows up — that would need
something like a stored API token or session cookie Fleaflicker doesn't
appear to offer for this purpose.

## Taxi/IR — solved for real, via the activity feed (not FetchRoster)

After the login wall killed the `FetchRoster` approach, reading the
*entire* API docs page top to bottom (not just the endpoints already in
use) turned up `FetchLeagueActivity` — public, no login, and its
`reserveChange` items log every taxi/IR move.

Fleaflicker's JSON encoding omits boolean fields when they're `false`
(standard protobuf JSON behavior), so a `reserveChange` item's shape
*is* the event:
- no `taxi`, no `removed` → added to **IR**
- `taxi: true`, no `removed` → added to **TAXI**
- `removed: true`, no `taxi` → removed from **IR** (activated)
- `removed: true, taxi: true` → removed from **TAXI** (promoted)

Verified against 11 real players in a real feed response before this
was wired in — e.g. A.J. Brown's real NFL IR designation lines up with
a bare add-to-IR event; Denzel Boston and Harold Fannin, both real
active contributors now, show promoted-off-taxi events. All 11 matched.
Also verified the "latest event wins" logic against a player (Alec
Pierce) with two conflicting historical events, correctly resolved by
timestamp.

`api/sync.ts` now fetches this activity feed (paginated, fetched in
parallel rather than sequentially since a league can have 1000+ items)
alongside the existing bulk roster endpoint, and computes each player's
current taxi/IR status from their most recent `reserveChange` event.
**Trades and taxi/IR are both automatic now.** Cuts remain proposals —
that stays true regardless of how solid the rest of this gets.

## Cuts are buyouts now, not deletions

Confirmed rule change: a cut no longer removes the contract. It converts
into a `BuyoutContract` — same player, same team, but with an explicit
per-year cost schedule: for every year that would have remained on the
original deal, the buyout owes 50% of THAT year's own contracted salary,
rounded up separately for each year (a $5 year becomes a $3 buyout year
on its own — confirmed against Nick's exact example). This is a real
change from the old rule, which took the flat current-year salary and
applied it across all remaining years with one rounding at the end;
the new per-year rule can total meaningfully more on an escalating deal.

A buyout **always counts against the cap** (same as any contract) but
**never occupies a roster/taxi/IR spot** — `rosterCount()` explicitly
excludes it, and it's never set with taxi/IR years.

`buyoutScheduleFromCut()` in `src/lib/contracts.ts` computes this
schedule; both `cutContract` (team page, edit mode) and `confirmCut`
(FA Review page) now call it via a shared `toBuyout()` helper in
App.tsx instead of deleting the contract. `cutPenalty()` — the "if cut
now" preview column shown on every active contract — now returns the
sum of this same per-year schedule, so the preview matches what
actually happens on confirm.

Team pages show a new "Buyouts" section (same place as Taxi/Injured
Reserve) with player, this year's buyout cost, and years left — same
shape as the active-roster table, just without position/resign/trade
controls, which don't apply to a bought-out player.

`api/sync.ts` explicitly skips `kind: 'buyout'` contracts before ever
trying to match them against a live Fleaflicker roster — they're gone
from the league, on purpose, and matching them would just generate a
false "not found" cut proposal every sync.

## Pre-existing buyouts from the original spreadsheet — found and imported

Nick asked directly whether buyouts that predated this app (before the
recent Keandre Lambert-Smith cut) had been accounted for. They hadn't
— checked the actual import script and confirmed it only ever parsed
two side sections per team sheet, "TAXI SQUAD" and "INJURED RESERVE".
There was no buyout parsing at all.

Went back to the original `.xlsx` and searched properly this time —
first for "BUYOUT" (no space, found nothing), then "BUY OUTS" (with a
space, found it everywhere). Every team sheet has a "BUY OUTS" label
twice: once as part of a broken aggregate rollup (`#REF!` errors, no
per-player detail), and once as a real per-player side table with year
headers, structured exactly like the Taxi Squad / Injured Reserve
tables the original import already knew how to read.

Parsed that second table on all 10 sheets. **4 teams had real, currently-
relevant entries** — Denver Diamondbacks, South Bend Silver Hawks,
Strasbourg Soldiers, and Wakarusa Wizards, 14 players total, several
still carrying real 2025/2026 dollar amounts:

| Team | 2025 | 2026 |
|---|---|---|
| Denver Diamondbacks | $9 | $7 |
| South Bend Silver Hawks | $15 | $15 |
| Strasbourg Soldiers | $12 | $12 |
| Wakarusa Wizards | $3 | $0 |

None of this was ever counted in any cap-used number this app has
shown, for any team, until now. Added all 14 as `kind: 'buyout'`
entries directly in `src/data/realContracts.ts` (ids 234–247), using
the exact dollar figures from the spreadsheet's own table rather than
recomputing them — these predate the new per-year 50% rule entirely,
so they're historical fact, not something to re-derive.

## Getting new seed contracts into a live Redis deployment

Once the app has saved anything at all (it has — the Osos cut), Redis
holds the real, full contract list and the bundled `realContracts.ts`
is only ever used to seed a brand-new empty database. Adding new rows
to `realContracts.ts` in code — like the 14 pre-existing buyout entries
(ids 234-247) — and deploying does **not** make them appear on their
own; Redis's existing data overrides the file every time.

`api/merge-new-seed-contracts.ts` is the safe fix: it adds only the
contract ids present in `realContracts.ts` but missing from Redis,
and leaves everything already live (synced trades, confirmed cuts,
manual edits) completely untouched. **Not** the same as
`restore-seed-data.ts`, which wipes and replaces everything — never
use that once real data has accumulated.

After deploying this update, visit `/api/merge-new-seed-contracts`
once in a browser (GET, same as restore-seed-data) to pull the 14
buyout entries into the live data.

## Taxi squad: 4-spot max

`MAX_TAXI_SPOTS = 4` in `src/lib/contracts.ts`. Taxi already correctly
excluded from cap math before this (confirmed — `countsAgainstCap`
already checked `isTaxi`), so this only adds the spot limit itself.

Manually adding a 5th taxi player via Edit Roster is blocked outright
with an explanation. Fleaflicker sync is treated differently on
purpose: it's never blocked from setting taxi status, since it reflects
whatever's actually true on Fleaflicker. If a sync ever leaves a team
over 4, the team page flags it ("Over limit" badge, red-highlighted
count) and points at fixing it on Fleaflicker directly, rather than
silently overriding synced data to force a local number. Same
red-highlight treatment on the League Summary's Taxi column, so an
over-limit team is visible at a glance across the whole league.

## Real bug found and fixed: sync was silently clearing taxi/IR status

Nick caught this from two symptoms that turned out to be the same bug:
South Bend showing 3 taxi players instead of a real 4 (missing Kyle
Williams), and Boulder's cap used reading $194 against a manual count
of $192 — exactly the $2 salary of Cam Ward or Oscar Delp, both on
Boulder's original taxi list.

The bug, in `api/sync.ts`: `reserveMap.get(...) ?? { isTaxi: false,
isIR: false }`. Finding NO reserve-change event for a player was being
treated as a confirmed signal that they're not on taxi/IR — but a
player's taxi/IR status set during initial preseason roster
construction may never generate a `reserveChange` activity item at
all, since that tracking starts once the season's activity feed does.
"No event found" was silently overwritten as "definitely active,"
which put their full salary back against the cap.

**Fixed**: taxi/IR is now only ever changed for a player when a real
`reserveChange` event exists for them. No event at all means the
existing value — whatever it already was — is left completely alone.

This only stops it happening again — it doesn't undo the damage
already sitting in live Redis data. `api/repair-taxi-ir.ts` is a
one-time repair pass: for every live contract, if the original seed
(`realContracts.ts`) had a taxi/IR year that live data is now missing,
AND there's no confirmed reserve event for that player (same rule as
the fix), it restores that year from the seed. If a confirmed event
exists, that's trusted over the seed — this never overrides a real
move. Visit `/api/repair-taxi-ir` once after deploying, same as the
other one-time endpoints.

## Stadium sizing fixed, all 10 teams now have art

The header row used `align-items: stretch` with nothing capping the
stadium image's actual height — `.hero-slot-photo` only had a
`min-height`, and the image inside had `flex: 1` with no ceiling, so
it ballooned to its own natural size and stretched the whole header
with it. Fixed: `.hero-slot-photo` now has a real fixed `height: 130px`
(not min), and the image fills that fixed space instead of growing
past it.

Added the 4 remaining stadiums (all 10 teams now covered): Denver
Diamondbacks (Copperhead Coliseum), Elkhart Express (Railyard Arena),
Kansas City Kaiju (North Kansas City High Field — temporary, small
capacity on purpose per the image's own "temporary stadium" framing),
Strasbourg Soldiers (Stade de la Cathédrale). Source images were
1-3MB each — resized/compressed to ~230-260KB to match the existing
6 stadium images' scale, same as they'd need to be for a 130px-tall
thumbnail regardless.

## League history — all-time record, head-to-head, championships

Confirmed against real data before building: `FetchLeagueScoreboard`
games carry real `isChampionshipGame`/`isThirdPlaceGame` booleans, not
inferred — verified against 2018's actual championship (Grand Rapids
Growlers over Standale Stampede) and third-place game (Jenison
Juggernauts over Boulder Bandits), both pasted directly from a live
response. Franchise identity is tracked by `fleaflickerId` — never by
name, since team names have changed multiple times since 2018 (Growlers
-> Denver Diamondbacks, Juggernauts -> Olde Town Osos, Anchormen ->
Broad Ripple Big Horns, White Walkers -> Strasbourg Soldiers, Stampede
-> Kansas City Kaiju, Maulers -> Elkhart Express).

Two new endpoints:
- `api/compute-league-history.ts` — the heavy one. Loops every season
  2018-present, pulls standings (all-time record/points) and every
  week's scoreboard (head-to-head, championship detection) for each.
  Fetches one season at a time, parallel within that season's weeks —
  a deliberate concurrency cap (~150+ total requests) against rate
  limits and timeouts, at some cost to wall-clock time. GET-triggerable,
  same pattern as the other one-time endpoints. Caches the result in
  Redis under `ftfl:league-history`.
- `api/league-history.ts` — cheap read-only endpoint the frontend
  actually calls on every team page load. Never computes anything
  itself.

"Last place" per season is computed from worst regular-season record
(tiebreak: lowest points for) directly from standings — not from any
consolation-bracket flag, since no such field has been confirmed.
Verified the exact aggregation logic (last place, championship, 3rd
place winner/loser) against the real confirmed 2018 data before wiring
it into the endpoint.

Team pages are now a two-column layout below the header/cap bar: left
column keeps QB/RB/WR/TE, right column has the new history box, then
Injured Reserve, then Taxi, then Buyouts. Old team names show up
inline for any finish where the name differs from today's (e.g. "2018
(as Grand Rapids Growlers)").

**Not built yet, on purpose**: top scorer by position (career total)
per team. This needs boxscore-level data for every player, every week,
every season (~700+ additional requests) with correct attribution to
whichever team owned that player the week they scored — a much bigger
and riskier build than everything above combined. Shipping the
confirmed, tested piece now rather than bundling both into one
untested giant endpoint. The history box shows "Top scorer by
position — coming soon" as a placeholder.

**To activate:** deploy this, then visit `/api/compute-league-history`
once (takes a while — it's ~150+ external requests running in
sequence-of-parallel-batches). After that, every team page will show
real history automatically. Re-run that endpoint occasionally (e.g.
once a week during the season) to pick up completed games — it's not
automatic yet.

## Layout: full-width page, stadium as a wide landscape banner

`.page` was capped at 900px — way too tight once the two-column team
page (roster + history) needed room, causing table headers to wrap
awkwardly. Raised to 1600px.

Stadium art was squeezed into a 130px-tall strip alongside team
identity and colors, cropping landscape images down to almost nothing
useful. Restructured: team identity + colors are now a compact row on
their own, and the stadium photo is a full-width banner (220px tall)
below it, with the name/capacity caption overlaid via a gradient at the
bottom. Much better use of a landscape image.

## Top scorer by position (career) — the deferred piece, now built

Confirmed against a real boxscore response before writing any
aggregation code: `FetchLeagueBoxscore` uses the exact same
`groups`/`slots`/`leaguePlayer.proPlayer` shape already confirmed for
`FetchRoster` (same underlying type, reused) — and critically, BOTH
starting lineup and bench groups are present, which matters since a
career total needs every point scored regardless of whether a player
started that particular week.

`api/compute-top-scorers.ts` — independently re-fetches every season's
scoreboard (same discovery method as league history) to find real game
ids, then fetches every game's boxscore (~700+ requests total) and
attributes each player's points to whichever team's side of that
specific boxscore they appeared on — not their current team, so a
mid-season trade correctly splits credit between both teams for the
weeks each actually rostered them. Tested the core aggregation logic
(multi-week accumulation, bench-player inclusion, team isolation)
against synthetic known values before wiring it into anything real.

Separate endpoint, separate cache key (`ftfl:top-scorers`) — does not
depend on league history being computed first and doesn't share state
with it. `api/top-scorers.ts` is the cheap read-only counterpart the
frontend actually calls.

**To activate:** visit `/api/compute-top-scorers` once after deploying
— this is the heaviest job in the whole project, expect it to take a
while. Team pages will show real top scorers automatically once it
completes.
